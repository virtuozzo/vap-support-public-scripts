##### SMTP host and port #####
# Use protocol smtp (usually ports 25, 587) or smtps (usually port 465)
# Examples:
# SMTP_URL="smtp://smtp_server:587"
# SMTP_URL="smtps://smtp_server:465"
SMTP_URL=""

##### SMTP credentials #####
# Use --netrc-file https://everything.curl.dev/usingcurl/netrc
# or specify credentilas directly using semicolon
# Example:
# SMTP_CREDS="user:password"
SMTP_CREDS=""

##### SMTP from user #####
# Better to use 'smtp.mail.from' (taken from JCA (settings)) as SMTP_FROM to avoid send errors
# At the same time reports will have server name as a sender which simplifies source determination
SMTP_FROM=""

# For secure connection an additional --ssl-reqd option can be required

curl -s --connect-timeout 30 --url "$SMTP_URL" --user "$SMTP_CREDS" --mail-from  "$SMTP_FROM" --mail-rcpt `sed "s/,/ --mail-rcpt /g" <<<"$to"` --upload-file $mailTmp

